#include "main.h"

/**
 * _isupper - function that checks for uppercase letters
 * @c: character to be checked
 * Return: if uppercase returns 1 and 0 if otherwise
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
return (0);
}
