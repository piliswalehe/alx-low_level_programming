#include "main.h"

/**
 * _isdigit - function that checks for numbers 0 to 9
 * @c: Number to be checked
 * Return: if digit return 1 and 0 if otherwise
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
return (0);
}
